This folder contains the text and other various static varables that only slightly affect the operation of the program.

PyCalc.cfg contains variables that need to exist after the calculator closes, but are then loaded again when the program reboots.
HelpText.cfg contains the text for the Help page of the main menu
